# Copy this file to ".env" and add your real Groq API key to enable the
# live SOHAY AI Assistant (Module 6). Get a free key at https://console.groq.com
#
# In Vite, only variables prefixed with VITE_ are exposed to the browser.
VITE_GROQ_API_KEY=your_groq_api_key_here
